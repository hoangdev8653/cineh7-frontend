export interface ITheaterSystem {
    id: string;
    name: string;
    logo?: string;
    description?: string;
}

export interface TheaterSystemDto {
    name: string;
    logo?: string;
    description?: string;
}
