export interface ITheater {
    id: string;
    name: string;
    address: string;
    phoneNumber: string;
    email: string;
}

export interface TheaterDto {
    name: string;
    address: string;
    phoneNumber: string;
    email: string;
}
